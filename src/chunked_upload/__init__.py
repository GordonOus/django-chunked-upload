"""
django-chunked-upload — Upload large files to Django in multiple chunks,
with the ability to resume if the upload is interrupted.
"""

__version__ = "2.0.0"

default_app_config = "chunked_upload.apps.ChunkedUploadConfig"
